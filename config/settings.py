BASE_URL = "https://vpmp-dev-web.ticvic.in"
LOGIN_URL = f"{BASE_URL}/login"
TIMEOUT = 5000
TOOL = "playwright"  # Change to "selenium" if you want to use Selenium
BROWSER = "safari"  # Change to "chrome" or "edge" if you want to use those browsers